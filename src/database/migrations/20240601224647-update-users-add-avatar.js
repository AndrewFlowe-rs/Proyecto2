'use strict';

module.exports = {
  async up(queryInterface, Sequelize) {
    // No necesitas hacer ningún cambio aquí ya que la columna ya existe en la base de datos
  },

  async down(queryInterface, Sequelize) {
    // No necesitas hacer ningún cambio aquí para revertir la migración
  }
};
